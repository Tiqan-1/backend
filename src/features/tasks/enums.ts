export enum TaskState {
    active = 'active',
    deleted = 'deleted',
}

export enum TaskType {
    lesson = 'lesson',
    assignment = 'assignment',
    oralExam = 'oralExam',
    meeting = 'meeting',
    wird = 'wird',
}
