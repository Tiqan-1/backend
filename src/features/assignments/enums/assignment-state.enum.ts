export enum AssignmentState {
    draft = 'draft',
    published = 'published',
    canceled = 'canceled',
    closed = 'closed',
    deleted = 'deleted',
}
export enum AssignmentType {
    exam = 'exam',
    homework = 'homework',
}
