FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies)
RUN npm ci

# Copy source code
COPY . .

# Expose the port
EXPOSE 3000

# Start in development mode
CMD ["npm", "run", "start:dev"]
